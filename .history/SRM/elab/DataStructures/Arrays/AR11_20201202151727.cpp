#include<bits/stdc++.h>
#include<iostream>
using namespace std;
int maxSum(int a[],int N,int k){
    return ((N));
}

int main(){
    int size,sum = 0;
    cin >> size;
    int arr[size];
    arr[0] = size;
    for(int i = 0; i < size; i++){
        int a;
        cin >> a;
        sum += a;
    }
    int a = maxSum(arr,sum,0);
    cout << sun/ << endl;
    return 0;
}