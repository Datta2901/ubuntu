#include<bits/stdc++.h>
#include<iostream>
using namespace std;

struct node{
    int data;
    struct node *next;
};

int main(){
    int size;
    cin >> size;
    struct node *temp,*head = NULL;
    for(int i = 0; i < size; i++){
        int data;
        cin >> data;
        struct node new_node =
        if(head == NULL){
            head = 
        }
    }
    return 0;
}