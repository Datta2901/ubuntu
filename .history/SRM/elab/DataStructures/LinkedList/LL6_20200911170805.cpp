#include<bits/stdc++.h>
#include<iostream>
using namespace std;

struct node{
    int data;
    node* next;
};

int main(){
    int size;
    cin >> size;
    node * arr[size];
    for(int i = 0; i < size; i++){
        int a;
        cin >> a;
        node* arr[i] = new node();
        arr[i]->data = 
    }
    return 0;
}