// deleted the nodes
#include<bits/stdc++.h>
#include<iostream>
using namespace std;

struct node{
    int data;
    struct node* next;
};

int main(){
    int size;
    cin >> size;
    while(size--){
        struct node* new_node =(struct node*)malloc(sizeof(struct bo))
    }

    return 0;
}