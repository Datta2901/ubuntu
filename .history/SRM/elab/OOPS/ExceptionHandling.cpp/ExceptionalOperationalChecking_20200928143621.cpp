#include<bits/stdc++.h>
#include<iostream>
using namespace std;

int main(){

    double a,b,c;
    char op;
    cin >> a >> op >> b;
    try{
    if(op == '+'|| op == '-' || op == '*' || op =='/'){

    }
    }

    return 0;
}