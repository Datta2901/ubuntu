#include<bits/stdc++.h>
#define EXECUTE_FASTER ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
using namespace std;
int main(){
    EXECUTE_FASTER
    int t;
    cin >> t;
    while(t--){
        int size;
        cin >> size;
        vector<long int> rating;
        for(int i = 0; i < size; i++){
            long int a;
            cin >> a;
            rating.push_back(a);
        }
        long long int  sum = max(a[1]+a[size.\];



    }
    return 0;
}