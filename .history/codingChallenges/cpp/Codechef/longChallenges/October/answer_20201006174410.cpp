#include<bits/stdc++.h>
using namespace std;

int getIndex(vector<int> numbers,int p){
    int minimum = INT_MAX,answer = 0;
    for(int i = 0; i < )
    return answer;
}
int main(){
    int n,x,p,k;
    cin >> n >> x >> p >> k;
    vector<int>numbers;
    for(int i = 0; i < n; i++){
        int a;
        cin >> a;
        numbers.push_back(a);
    }
    sort(numbers.begin(),numbers.end());

    
    return 0;
}