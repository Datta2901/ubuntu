#include<bits/stdc++.h>
using namespace std;

int main(){
    int size;
    cin >> size;
    vector<int> velocity,answer;
    for(int i = 0; i  < size; i++){
        int a;
        velocity.push_back(a);
    }
     
    for(int i = 0; i < )  

    return 0;
}