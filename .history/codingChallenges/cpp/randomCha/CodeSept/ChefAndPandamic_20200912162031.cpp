#include<bits/stdc++.h>
using namespace std;
int main(){
    int size;
    cin >> size;
    int arr[size + 1];
    arr[0] = 0;
    for(int i = 1; i < size; i++){
        cin >> arr[i];
    }
    int queries;
    cin >> queries;
    while(queries--){
        string s;
        int a,b;
        cin >> s >> a >> b;
        if(s =="Increase"){

        }
    }
}
