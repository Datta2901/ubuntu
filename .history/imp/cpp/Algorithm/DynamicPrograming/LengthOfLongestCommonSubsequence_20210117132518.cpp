#include<bits/stdc++.h>
using namespace std;

int LengthCIS(string a,string b){

    int L[a.size() + 1][b.size() + 1];
    for(int)

    return
}

int main(){
    string a,b;
    cin >> a >> b;


    return 0;
}