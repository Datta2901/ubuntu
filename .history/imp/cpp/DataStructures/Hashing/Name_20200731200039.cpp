#include<bits/stdc++.h>
using namespace std;

void insert(long int index,string s,HashT){

}

int main(){
    int number;
    cin >> number;
    vector< HashTable[number];
    while(number--){
        long int roll;
        string name;
        insert(roll,name,)
    }
    return 0;
}