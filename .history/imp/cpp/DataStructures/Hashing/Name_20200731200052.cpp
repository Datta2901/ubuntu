#include<bits/stdc++.h>
using namespace std;

void insert(long int index,string s,vector<HashT){

}

int main(){
    int number;
    cin >> number;
    vector<string> HashTable[number];
    while(number--){
        long int roll;
        string name;
        insert(roll,name,)
    }
    return 0;
}