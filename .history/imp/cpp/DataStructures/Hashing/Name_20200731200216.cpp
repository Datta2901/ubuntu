#include<bits/stdc++.h>
using namespace std;

void insert(long int index,string s,vector<string> HashTable[],int size){
        index = index % 
}

int main(){
    int number;
    cin >> number;
    vector<string> HashTable[number];
    while(number--){
        long int roll;
        string name;
        insert(roll,name,HashTable,);
    }
    return 0;
}