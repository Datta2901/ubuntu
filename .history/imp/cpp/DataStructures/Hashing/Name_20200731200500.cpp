#include<bits/stdc++.h>
using namespace std;

void insert(long int index,string s,vector<string> HashTable[],int size){
        index = index % size;

}

int main(){
    int number;
    cin >> number;
    long int  size = 1
    vector<string> HashTable[int(1e9 + 1)];
    while(number--){
        long int roll;
        string name;
        insert(roll,name,HashTable,number);
    }
    return 0;
}