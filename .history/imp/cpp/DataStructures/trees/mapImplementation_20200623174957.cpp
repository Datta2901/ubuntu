#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main(){
    map<int,int> Node;
    int a,b; 
    cin >> a >> b; // it reprsent they are parent or child
    Node[a] = b;

    return 0;
}