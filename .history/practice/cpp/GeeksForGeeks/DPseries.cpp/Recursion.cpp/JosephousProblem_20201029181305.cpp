#include<bits/stdc++.h>
using namespace std;

int main(){
    int size,personT0Kill;// given number of persons are there in the arrangement
    cin >> size >> personT0Kill;

    cout << GetTheSafestPerson(size,)
    return 0;
}
