#include<bits/stdc++.h>
using namespace std;

double myPow(double x, int n) {
    double answer = pow(x,n);
    return answer;
}

int main(){
    double x;
    int n;
    cin >> x >> n;
    coumyPow(x,n);
    return 0;
}