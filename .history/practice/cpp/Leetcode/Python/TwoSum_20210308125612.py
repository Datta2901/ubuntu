class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        ha = {}
        for i in nums:
            if i in num:
                num[i]++
            else:
                num[i] = 1
        answer = []
        for i in 
           
        