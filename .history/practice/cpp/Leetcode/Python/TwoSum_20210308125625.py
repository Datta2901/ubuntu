class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hash = {}
        for i in nums:
            if i in hash:
                has[i]++
            else:
                num[i] = 1
        answer = []
        for i in 
           
        