class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        answer = []
        for i in range(0,nums):
            if (target - num[i]) in nums:
                answer.append(i,get)
           
        