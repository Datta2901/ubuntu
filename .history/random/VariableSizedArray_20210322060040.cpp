#include<bits/stdc++.h>
#include<iostream>
using namespace std;

int main(){
    vector<vector<int>> numbers;
    int size,queries;
    cin >> size >> queries;
    for(int i = 0; i < size; i++){
        int s;
        cin >> s;
        for(int j = 0; j < s; j++){
            int a;
            cin >> a;
            
        }
    }

    return 0;
}