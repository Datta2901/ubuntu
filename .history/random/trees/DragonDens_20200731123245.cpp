#include<bits/stdc++.h>
using namespace std;

const int N = 200005;
vector<int> Ltrav[N],Rtrav[N];
int par[2][N],st[2][N],ed[2][N];
int timer = 0;

int main(){
    int n,q;
    cin >> n >> q;
    int h[n],a[n];

    memset(par,-1,sizeof(par));

    for(int i = 0; i < n; i++){
        cin >> h[i];
    }
    f
    return 0;
}